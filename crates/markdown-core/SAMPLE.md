# Markdown WYSIWYG

A paragraph with **bold**, *italic*, and `inline code`, plus a
[link](https://example.com) to somewhere far away so the line is long
enough to wrap inside the pane and exercise the soft-wrap caret mapping.

## Lists and quotes

- first item
- second item with *emphasis* inside it
- third item

> a block quote line that says something thoughtful

### Code

```rust
fn main() {
    let x = 41 + 1;
    println!("hello, {x}");
}
```

---

Some closing text after the rule. Type `Cmd+/` to toggle the raw view.
