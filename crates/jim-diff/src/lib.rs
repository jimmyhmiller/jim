//! diff-bevy — Bevy integration for the diff-viewer pane.
//!
//! Scaffold only: the crate's dependencies (diff_core, jim_pane,
//! jim_style, jim_editor) are wired up in Cargo.toml but no diff
//! pane has been implemented yet. This empty library exists so the
//! workspace builds; add the pane plugin here when ready.
